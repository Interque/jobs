https://git.heroku.com/guarded-everglades-4684.git
http://jobs.interque.co

Toggle the: config.action_mailer.default_url_options = { host: 'localhost', port: 3000 } for production

.

Note: Be careful updating the vendor files for jquery tokenInput.
To get the autocomplete to work w/ bootstrap modals, manually overwrote the zindex in the css files.
